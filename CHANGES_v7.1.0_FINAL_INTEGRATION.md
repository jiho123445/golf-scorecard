# v7.1.0 — 최종 분석 통합판

지금 실제 GitHub 저장소(v6.4.1 기준)를 직접 열어 분석한 결과를 반영해, 그동안 설계만 되고
실제로 반영되지 않았던 항목들과, 이번 분석에서 새로 발견된 코드 품질 이슈까지 한 번에
통합했습니다. 이 zip을 그대로 저장소에 덮어쓰기 하시면 됩니다.

## 이번에 새로 반영된 것 (지금까지 미반영 상태였던 것)

1. **비밀번호 재설정** — 로그인 화면 "비밀번호를 잊으셨나요?" 링크, `AuthContext`에 `resetPassword` 추가
2. **이메일 인증** — 가입 시 자동 발송 + 설정 화면 미인증 안내 배너 + 재전송 버튼
3. **표시 이름 기기 간 동기화** — `useProfile` 훅으로 Firestore(`users/{uid}`)와 동기화, 기본값을 "골퍼"로 수정(기존 "김지호" 하드코딩 제거)
4. **Firestore 보안 규칙 강화** — `holeCount`/`sportType`/`status` 등 필드 타입·범위 검증 추가 (⚠️ Firebase 콘솔에 별도로 게시해야 합니다 — 아래 배포 체크리스트 참고)
5. **Firestore 오프라인 캐시** — `persistentLocalCache`로 골프장 약전파 환경 대응
6. **PWA 오프라인 지원** — `public/sw.js`, `public/offline.html` 추가 + `main.tsx`에 서비스워커 등록 (지금까지는 `manifest.webmanifest`만 있고 실제 서비스워커가 없어 "홈 화면에 추가"는 됐지만 오프라인 캐싱은 동작하지 않았습니다)
7. **관리자 회원 검색** — `AdminUsers` 화면에 이메일 검색창 + "전체 N명 · 검색 M명" 카운터
8. **개인정보처리방침 / 이용약관 페이지** — 설정 화면 하단에 링크 추가 (⚠️ `LegalPage.tsx`의 `【】` 부분은 실제 운영자 정보로 반드시 교체해주세요)

## 이번 분석에서 새로 발견해서 함께 고친 것

9. **CI 워크플로우 파일명 정리** — `.github/workflows/webpack.yml` → `ci.yml`로 변경 (내용은 동일, 이름만 정리)
10. **TypeScript `strict` 모드 활성화** — `tsconfig.app.json`에 `"strict": true` 추가. 다행히 기존 코드가 깨지지 않고 그대로 통과했습니다.
11. **`any` 타입 제거** — `App.tsx`의 `startRound` 함수가 `data: any`로 타입 체크를 우회하던 부분을 `NewRoundInput` 타입으로 교체

## 확인된 사항 (전부 통과)

- `npm run build` (tsc -b --strict && vite build) 정상 통과
- `npx oxlint` — 기존에도 있던 무관한 경고 4개 외 신규 이슈 없음
- `npx vitest run` — 16개 테스트 전부 통과
- `api/*.ts` 3개 파일 별도 strict 타입 검사 통과

## 배포 전 체크리스트

1. 이 zip의 파일들을 저장소에 덮어쓰기 → 커밋 → 푸시 (Vercel 자동 배포)
2. **Firebase 콘솔 → Firestore Database → 규칙(Rules) 탭에 `firestore.rules` 내용을 붙여넣고 "게시"** — 이번 배포에서 유일하게 Vercel 밖에서 별도로 해줘야 하는 단계입니다.
3. `src/components/LegalPage.tsx`의 `【】` 부분을 실제 운영자명/연락처/시행일로 교체
4. 배포 후 확인: 로그인 → 라운드 저장 → 이름 변경(다른 기기에서도 확인) → 비밀번호 찾기(테스트용 이메일로) → 이메일 인증 메일 재전송 → 관리자 화면 검색 → 개인정보처리방침/이용약관 페이지 열림 확인
5. 참고: `.github/workflows/webpack.yml`은 이제 필요 없으니, 새 `ci.yml`을 올리신 뒤 저장소에서 `webpack.yml` 파일은 삭제해주세요(둘 다 남겨두면 같은 검증이 두 번 실행됩니다).

## 이번에도 포함하지 않은 것 (그대로 보류)

- **npm audit 취약점 강제 수정**: `firebase-admin` 다운그레이드 위험 때문에 여전히 보류합니다.
- **전국 골프장 데이터 확장(72→541건)**: 데이터 검증 작업이 커서 별도 진행을 권장합니다.
- **App.tsx 코드 스타일 정리(세미콜론 압축 스타일)**: 동작에는 문제가 없고, 지금 손대면 diff가 커져서 리뷰가 어려워지므로 이번엔 손대지 않았습니다. 앞으로 기능이 더 늘기 전에 Prettier 등으로 한 번 정리하는 걸 권장합니다.
- **화면 단위 코드 스플리팅(React.lazy)**: 지금 규모에서는 체감 이득이 적어 보류합니다.
